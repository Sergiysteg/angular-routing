import { PhoneSearchPipe } from './phone-search.pipe';

describe('PhoneSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
